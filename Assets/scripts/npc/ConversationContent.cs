using System;
using System.Collections.Generic;

public class ConversationContent
{

	// index of current place in conversation (1, 2, 3)
	// content of conversation line ("Hello there!")
	// eg: 	ConversationContent c = new ConversationContent();
	//		c.AddCell(1, "Hello there!");
	//		void OnMouseDown () { string content = GetContent(2); }
	//		c.AddCell(2, "Bye!");
	// when making dialogue choices, call:



}